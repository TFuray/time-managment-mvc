const Event = require('../models/Event')    

module.exports = {
    getEvents: async (req, res)=>{
        try {
            const eventItems = await Todo.find()
            
        } catch (err) {
            
        }
    }
}